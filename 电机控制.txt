类比：
P=FV
P=扭矩*角速度---功率一定--角速度越小，扭矩越大



程序中函数内的局部变量--占用的是栈大小--栈大小在函数执行完就释放了，所以单片机的stack是1k
函数内部不能直接定义占1024字节的数据，否则就会发生core dump

malloc占用的是内存，在linux系统中，一个程序如果malloc了1G byte的内存，不释放，则系统中可用内存就会减少1G

1.全局变量和局部静态变量是存储在静态存储区的，默认值为0
2.局部变量数组未初始化,则其默认值为-858993460(垃圾值); 
  局部变量处于堆栈区,其数值是随机的---
  **以后记住局部变量一定要init
  
sqrt c求平方根返回整数结果
pow(x,n)  求x的n次方结果